apiVersion: v1 
kind: PersitentVolumeClaim
metadata:
  name: mysql-pvc 
  namespace: todo3tier 
  labels:
    app: mysql-pvc 
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi